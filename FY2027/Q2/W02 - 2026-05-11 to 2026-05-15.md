# Work Log - 2026-05-11 to 2026-05-15

**Fiscal Year:** FY2027 Q2 Week 2

---

## Notes

### Monday



### Tuesday



### Wednesday



### Thursday



### Friday

Spent most of the day on two things. First, dug into why the Tetrate FIPS build tag lookup in the service-mesh upgrade-planner skill was broken — the releases page is client-side rendered so WebFetch only gets an empty HTML shell. Found a public JSON API at `tis.tetrate.io/versions.json` that has all the release metadata, updated the skill to hit that directly, and opened PR #274.

Also investigated a wave of PRs that were deleting and recreating IAM resources whenever team names changed. Initially suspected `terraform-aws-autoscaling-group` but the team name only flows through as a tag there, not into resource names. The actual issue is `terraform-aws-instance-profile`, which the security team owns — it hardcodes team names into IAM resource names. Created a Jira ticket for decoupling that, opened a draft PR in that repo as an example implementation, and put together a design for a phased migration. Sent a message to security to loop them in.

Also spun up the Istio 1.29 Upgrade 1-pager and project folder in Confluence.

