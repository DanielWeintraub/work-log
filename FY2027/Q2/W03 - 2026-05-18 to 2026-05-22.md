# Work Log - 2026-05-18 to 2026-05-22

**Fiscal Year:** FY2027 Q2 Week 3

---

## Notes

### Monday

Did some work in terraform-aws-autoscaling-group to promote it from draft. Cleaned it up in preperation for Neha getting back from PTO.

Tried to get started on the istio upgrade, but most of my time was spend with a on-call request regarding a cert renewal. I had to set up local knife access but the docs were bad so I had to update them.

### Tuesday

Finished the identity cert rotation. Went through load-test (identity.cloudoverflow.com), staging (identity.pd-staging.com), and prod + euprod (identity.pagerduty.com). Had to add a one-liner to the SSH config to rewrite .pd-internal.com hostnames to .pd so knife ssh would work through the SSM proxy. Ran cinc-client + nginx reload on felb hosts across all environments and verified with openssl. Wrote up CLI instructions and added them to the 2026 TLS cert renewals page, then created a separate page with my notes from the process.

Also reviewed chef PR #18032 for a security incident. This PR was an FELB Dockerfile fix switching from a hard-coded Cloudsmith entitlement token to the existing build credentials. The mount wiring looked correct.

Tried to test my istio upgrade skill PR. Had some friction getting the plugin to use my local checkout instead of the cached version.

### Wednesday

Core Infra oncall operational review meeting. After that, did a retrospective review of web-priv-load-balancer PR #265 (CORE-4688 container resource fix) — it was already merged but the team wanted eyes on the CPU limit values across environments. Also fielded a question about whether Karpenter disruption operations run on a schedule or 24/7 — traced through the disruption budgets config and confirmed it's 24/7 since there's no `schedule` block. Wrapped up by cleaning up my identity cert rotation notes page.

Also two fielded two on-call queries in the channel. One related to consul in Gov Cloud and one related to pd-python in Gov Cloud.

### Thursday

Finished the identity cert rotation. Rotated identity.pd-staging.com and identity.cloudoverflow.com via ACM export (staging account) and updated the Chef databags. Prod and euprod were from 1Password — a lot of fiddly openssl work splitting bundled certs and stripping \r characters. Got through all environments. Also filed CORE-4726 and opened knife-chef-config PR #21 to add *.pem and *.key to .gitignore since those weren't covered.

Also reviewed pd-python PR #116 (RLSE-1096) — adds GovCloud ECR publishing to us-gov-east-1. Main finding was that atlantis-gov applied to fedprod before any approval landed, which is a gap in the apply_requirements config. Prodded Andy and he took over.

Spent a bit of time tracing through pd-kubectx-cli's pk role command to understand what network calls it makes on role switch. Would like to improve this.

### Friday


