# Work Log - 2026-06-01 to 2026-06-05

**Fiscal Year:** FY2027 Q2 Week 5

---

## Notes

### Monday

Spent most of the day on two repos. In atlantis-local-cli, kicked off a Python 3.14 upgrade (`.tool-versions`, `Pipfile`, `setup.py`, Buildkite image) and opened PR #93. Also unblocked a stale Renovate PR (#82) where the `ecr` and `bogus` Terraform environments were missing `versions.tf` and Atlantis was trying to run them on 1.6.4 instead of 1.9.x — added the files, pushed, and got #82 merged. Addressed review feedback from Martin on #93 (forgot to bump `setup.py`'s `python_requires` and he wanted a Homebrew note in CLAUDE.md), then that got merged too. Also wrote up the Istio 1.29 upgrade 1-pager in Confluence.

In pd-dns, was deep in CORE-4582 — PR #202 to deduplicate R53 inbound resolver subnets by AZ in the RBA accounts. Did a thorough review pass, added a precondition guard and some comments, then did a full org-wide AWS search to confirm nothing hardcodes the old IPs. CloudWatch showed zero inbound queries to all three RBA resolver endpoints over 30 days, which made the risk picture a lot cleaner. Triggered an Atlantis apply but it timed out mid-run both times on staging — the ENI provisioning just takes long enough that the STS token expires. Sandbox applied fine. Switched to manual AWS CLI with the SRE profile to drive staging by hand; got the first disassociate in before calling it a day.


### Tuesday

Cleaned up several repos.

Improved atlantis-cli. Investigated the pd-dns issues.

### Wednesday

Finished off the long running pd-dns problem. The Atlantis apply was failing on staging because the provider was hitting a `LimitExceededException` — it was trying to associate a new IP before removing old ones, and the endpoint was already at 6. Drove the whole thing manually with `rba-sre` via AWS CLI: disassociated the old IPs, associated the new subnets, waited for OPERATIONAL between each step. Did the same for prod. Final Atlantis plan across all 9 projects came back `0 to add, 0 to change, 0 to destroy` — PR #202 is merged.

Also spent some time researching Cinc EOL in the chef repo. Client 18.x dropped active support in February, server 15.x hits EOL in November. Chef 360 is the upstream replacement but it's fully commercial — Cinc hasn't announced anything to fill that gap, which is probably the answer in itself.

### Thursday

Did atlantis stuff. Not really sure.

### Friday

Big day in atlantis-local-cli. Designed and implemented multi-instance support so `atl` targets commercial Atlantis and a new `atlg` / `atlantis-gov` binary targets govcloud — no flags, no prompts, instance is just determined by which binary you ran. New `atl_instances.py` as the single source of truth for per-instance config, `atl auth -a` to refresh both. Also built out all the govcloud deployment infra: k8s Helm chart + ArgoCD manifest targeting `eks-fedprod-iss-usgw1`, Terraform `fedglobal` and `ecr-govcloud` environments, `atlantis-gov.yaml`, CircleCI config for the fed ECR push. Opened PR #100 (Python changes) and PR #101 (infra). Worked through a bunch of Copilot review findings on #100 — missing `instance=` parameter passing, a missing `return` after retry in `atl_get`, `_active_instance` not set before the WebSocket connection opens, and a couple of smaller cleanups.

Also hit a packaging bug that's been lurking since v1.0.0: the autologin success page was read from a relative path that only worked in a dev checkout, not a Homebrew install. Fixed by symlinking `index.html` into the Python package and switching to `importlib.resources` — opened PR #103. Then bumped the Homebrew formula to v1.1.1 (PR #192 in homebrew-pd_brews), which also hit a fun detour where `runs 1.3.0` switched to a Rust build backend — pinned it to `1.2.2`.

Did code reviews for a couple other repos: aws-sumo-vpc-flow-logs PR #24 (euprod Loki forwarder cross-account grants) and sre-workstation-infrastructure PR #47 (U22 FedRAMP ASGs, caught a malformed `module "common"` block).

