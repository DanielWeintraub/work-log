# Work Log - 2026-06-08 to 2026-06-12

**Fiscal Year:** FY2027 Q2 Week 6

---

## Notes

### Monday

Also created CORE-4750 and fixed the cloudflared FIPS deb upload issue — the script was uploading the .deb once, getting a single Cloudsmith identifier, then trying to publish two packages (focal and jammy) from the same identifier. Cloudsmith consumes the identifier on first use so jammy was silently failing. Moved the upload inside the dist loop so each distro gets its own upload. PR #18087 in chef. Reverted the earlier workaround PR (#18084) that just duplicated the pipeline step.

Reviewed gateway-infrastructure PR #74 (Ubuntu 22 shadow ASGs for OS upgrade testing) and aws-sumo-vpc-flow-logs PR #24 (cross-account S3/SNS grants for the eu-prod Loki forwarder). For #74 flagged the ssm-ssh-proxy instance discovery issue — when the u22 ASGs scale up, `*infra-gw*` matches both old and new instances and routes SSH to the most recently launched one. For #24 initially flagged a wrong provider on the euw1 SNS subscription but walked it back after checking the module's provider alias setup.


### Tuesday



### Wednesday



### Thursday



### Friday

Continued rolling out Istio 1.29. It's now in everywhere but prod and euprod. Also set default to 1.29 in playground clusters.

Had a meeting regarding what we should do about our services and the graduated rollout effort.