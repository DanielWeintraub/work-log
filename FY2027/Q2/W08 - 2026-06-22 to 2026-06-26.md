# Work Log - 2026-06-22 to 2026-06-26

**Fiscal Year:** FY2027 Q2 Week 8

---

## Notes

### Monday



### Tuesday



### Wednesday



### Thursday



### Friday

Spent most of the week on the Istio 1.29 upgrade (CORE-4737). Merged PRs to deploy istiod 1.29.3 to prod and euprod. Also merged PRs to flip the default Istio tag from 1.27.1 to 1.29.3 across playground, load test, fedstaging, and staging tiers.

Also wrapped up CORE-4774 — merged the TAEC PR (#882) that switches the istiod PDB from `minAvailable: 1` to `maxUnavailable: 1` and sets `rollingMaxSurge` to 20%. That came out of investigating the June 16 incident on eks-euprod-pd-euc1 where a routine TAEC version bump triggered a full istiod rolling restart and caused a ~53s 5xx blip. Spent some time with Tetrate support working through the surge/unavailable/minReadySeconds behavior. Also added an `istio_preserve_metric_rotation_interval` variable to gate removal of the metric env vars from prod clusters so we don't accidentally trigger another restart — that was connected to the June 2 euprod incident too. Created a Datadog dashboard to track istiod rollouts across clusters.

