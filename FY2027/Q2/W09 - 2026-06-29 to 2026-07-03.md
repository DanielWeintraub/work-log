# Work Log - 2026-06-29 to 2026-07-03

**Fiscal Year:** FY2027 Q2 Week 9

---

## Notes

On-call for the week
 
### Monday

Spent most of the day pushing the Istio 1.29.3 canary rollout across prod clusters. Moved the canary tag to 1.29.3 on EU prod (CORE-4818), US prod (CORE-4820), and fed prod (CORE-4822) — verified the fed prod apply via kubectl since I had to target it manually. Also created the corresponding default-tag PRs for all three environments (CORE-4819, CORE-4821, CORE-4823), held pending canary validation. Marked CORE-4774 done too — that's the istiod PDB fix to cap disruption to one pod at a time so we don't get another synchronized sidecar reconnect storm.

Also knocked out a bunch of TAEC v31.1.1 bumps. CORE-4824 covered stg, lt, fedstg, and sandbox (several PRs merged), CORE-4827 got euprod done, and CORE-4828 is in progress for the main prod clusters.

On top of that was the pd-oncall-chat-topic Lambda work. CORE-4825 was the hotfix for the "Deactivated User" bug caused by PD's new shift-based schedule format breaking the v2 overrides endpoint. CORE-4826 is the fuller fix with v3 API support — that's in ready for review now.

### Tuesday

Lot of code review today. Looked at a few chef PRs — SSE support for workbench-service-api on the FELB nginx config, flipping websocket and connection-header feature flags to true in prod and euprod, and a least_conn change for slack-service in load test. Also reviewed a pd-dns PR enabling DNSSEC validation for the fedprod/fedstg Route 53 resolvers, a felb-infrastructure PR adding an ALB into the FELB chain for the commercial load test usw2 environment, and a terraform-aws-network-security PR adding a WAF Web ACL for staging/lt. Spent some time digging into the terraform-aws-eks-cluster Istio module to understand how the dual-version revision setup actually works.

Also finished off the last of the TAEC v31.1.1 wave — more stg/lt cluster PRs merged (CORE-4824 done), euprod clusters done (CORE-4827), and rdc prod merged (CORE-4828, still in progress). The pd-oncall-chat-topic hotfix from Monday got deployed to prod via the infra redeploy PR, so CORE-4825 is fully closed. Plus knocked out CORE-4831 — upgrading pd-manifester's CircleCI config from v2 to v2.1 before their August EOL deadline; that's in ready for review.

### Wednesday



### Thursday



### Friday


