# Work Log - 2026-07-06 to 2026-07-10

**Fiscal Year:** FY2027 Q2 Week 10

---

## Notes

### Monday

Out for Medical

### Tuesday

Out for Medical

### Wednesday

Finished off the Istio 1.29.3 rollout — flipped the default tag on US prod (CORE-4821) and fed prod (CORE-4823). All merged and applied clean, no sidecar injection issues after.

Also opened two tickets for the noisy Consul raft monitor from last week's on-call. CORE-4854 covers the actual fix — the window/aggregation setup was letting a single 10s sample hold the monitor in ALERT for a full 15 minutes, which is why it kept resolving at almost exactly :15 every time. Turned out to just be normal gossip/RPC blips, not a real cluster problem. While I was in there I noticed the sibling monitor raft_apply_times_high had no env/region tag filter at all, so every environment's copy was alerting on the same global metric — filed CORE-4855 for that as a quick follow-up. Both are in ready for review.

### Thursday

Big day of Istio cleanup. Opened and merged a bunch of PRs to remove the old 1.27.1 istiod from stg, lt, fedstg, and playground clusters (CORE-4857) plus euprod (CORE-4858). Prod is blocked for now. Also updated the default `istio_versions` in terraform-aws-eks-cluster to 1.29.3 so new clusters come up on the right version by default (CORE-4860). With 1.27.1 fully gone from all non-prod clusters, marked the Istio upgrade M2 epic (CORE-4715) done, and closed out the two lingering istio-mirror/install-cni vuln tickets (CORE-4511, CORE-4387) that were blocked on the upgrade.

Also merged the Consul monitor fixes from Wednesday — CORE-4854 and CORE-4855 both landed. And CORE-4826 got closed too, the pd-oncall-chat-topic v3 API upgrade. Plus CORE-4831 (pd-manifester CircleCI v2.1 bump) closed. Plus CORE-4862, which was the fleet roll script I added to sre-scripts to make future Istio upgrades less painful.

On top of all that, spent some time investigating an open incident from last week's on-call — an lt-adminlb instance had a 100% full disk which was causing the topology-manager service to crash-loop. Got into it via SSM since SSH was broken (couldn't write session files with 0 bytes free).

### Friday

Finished CORE-4862 — the istio-fleet-roll script is merged to sre-scripts (PR #216) and the Confluence upgrade runbook is updated to point at it. The script runs in summary mode with just a context, or generates restart commands for workloads still on an old revision when you pass a version argument. Defaults to dry-run, `--execute` to actually do the restarts with a configurable pause between. Updated the Fleet Roll section of the Istio Upgrade Step-by-Step page to document it.

Also got the last of the non-prod 1.27.1 cleanup done — fedprod clusters PR #2017 merged (CORE-4857). CORE-4860 closed (default istio_versions in TAEC bootstrap now points at 1.29.3). CORE-4828 (TAEC v31.1.1 for prod) and CORE-4826 (pd-oncall-chat-topic v3 API) both closed out with their final PRs. CORE-4859 (remove 1.27.1 from prod) is still blocked, and CORE-4829 (M3 post-upgrade hardening epic) moved to in progress.

The only thing left is removing istio 1.27 from prod us-west-2 when all the sidecars migrate over. I briefed Anthony on this and he can take care of it.