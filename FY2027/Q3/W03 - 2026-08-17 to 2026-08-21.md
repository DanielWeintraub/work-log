# Work Log - 2026-08-17 to 2026-08-21

**Fiscal Year:** FY2027 Q3 Week 3

---

## Notes

### Monday

Wrapping up on-call work. Merged some cads renovate work. Wiz isn't seeing the fix, investigating. Can't connect Wiz MCP, reached out to helpdesk. Resolved 12 incidents, manually deleted buran surfaced dns records since the runner was down in fedstg.

Created new script to list all core-infra repos. Finished infra-team-llm-config setup script.

### Tuesday

Verified the pygments fix for CORE-4647 (the Wiz-flagged CVE-2026-4539 on cross-atlantic-discovery-service). Didn't just trust the lockfile — pulled the actual built ECR image via prod-ro and confirmed pygments 2.20.0 at runtime. Also got the CORE-4949 fix merged for aws-vpc-flow-logs (backend state key mismatch after the repo rename), though most of the time went into digging through Atlantis plan history to figure out which of two old PRs had actually been applied to staging before I felt safe merging. Devlin later found that there's another repo that's misconfigured that was fighting with this repo's configuration.

Spent a while auditing core-infra repos for old terraform-aws-autoscaling-group versions after a new diagnostic monitor shipped in v13.5.0 — 9 of 66 repos are behind, hami_builder_u20 worst at two majors old. While checking why Renovate hadn't already opened that bump, found it's not just schedule-gating like I first assumed: Renovate looks genuinely stuck on that repo since July 20, no activity across four weekly windows while other repos update fine. Opened CORE-4971 to chase that down properly.

### Wednesday



### Thursday

Reviewed a couple of homebrew-pd_brews PRs. Approved the pd-aws formula bump to 1.1.3. While testing it, my SSH to euprod-chef-server15 was broken — traced it partway, the proxy connection through euprod-gw.pd was dying during kex_exchange_identification, never fully nailed down whether the jump host or the target was at fault. Also ended up untangling a stray AWS Session Manager plugin install — the signed pkg installer ignores `-target` and always drops it in /usr/local regardless of what path you ask for, so it wasn't where I expected. Cleaned that up and figured out the bundled zip installer is the one that actually respects a custom install path. Requested changes on the pd-ssh-autocomplete PR (#196) too.

Focused on Core Infra repo cleanup and ownership.

### Friday


