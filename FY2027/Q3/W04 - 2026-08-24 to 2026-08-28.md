# Work Log - 2026-08-24 to 2026-08-28

**Fiscal Year:** FY2027 Q3 Week 4

---

## Notes

### Monday



### Tuesday

Spent much of the day on CORE-4987, handing the dockerfiles repo over to Developer Experience. Added a CODEOWNERS file, fixed a couple of stale sre-cloudinfra owner labels, and pulled out a dead CircleCI ECR orb reference along with the nightly image-build automation, which turned out to have been silently broken (wrong branch name) for a while anyway. PR #172 merged, but CircleCI kept posting a red X on it afterward from an orphaned project registration — tracked that down and deleted the CircleCI project via their API to make it stop.

Also reviewed a few PRs. On service-mesh-http-owl #48, found that the host_suffix option isn't locked down the way port is, which undermines the security fix the PR is supposed to be making. On atlas #195 I actually checked out the branch and ran the tests instead of just reading the diff — the PR description claims 26 tests but only 20 exist. #196 was the bigger one, moving atlas onto MySQL/Aurora — tested it hands-on against real Postgres and MySQL containers and found a real regression: CIDR/IP field validation quietly disappears when the field type changes for MySQL compatibility. Talked through a fix with the author.

Poked around pd-status-setup too — still very much in use, just gets invoked through the pd-status-buildkite-plugin rather than run directly. Also ran the security-ticket-finder script against Jira: 37 open findings, one overdue (CORE-4179).

I also put together a spreadsheet detailing repo ownership in preparation for the service shuffle.

### Wednesday

wormly-sync started crashing on every 10-minute run early this morning, breaking backup-paging sync for every team, not just the one that triggered it. Turned out the PagerDuty on-call API returned a user reference with no name for a deactivated account, which flowed into a `nil.start_with?` call and blew up. Worse, there was no error isolation between team configs, so that one bad on-call entry on mobilization's schedule silently skipped sre-cloudinfra, im-incidents, dbre, and a bunch of others too. Fixed both: skip/log unresolvable on-call names, and wrap each team config's sync so one failure doesn't take down the rest. CORE-4988, PR #180 merged.

Cause was layoffs, so spent much of the day processing that.

Also circled back on the atlas #196 review from yesterday to check whether the CIDR/IP validation regression I'd flagged was actually fixed. Left a couple follow-up comments on the test file and approved once I confirmed it.

### Thursday

Fixed a gap in my own `/summarize-day` skill — it only ever checked PRs I'd merged, which is exactly how Wednesday's atlas #196 review slipped through the cracks. Added a step that searches for PR reviews too, cross-checks the timestamps against the real review API so it doesn't get fooled by stale "updated" dates, and pulls in line comments for context. Committed that.

Also moved a few CORE tickets along. CORE-4938 (disabling StrictHostKeyChecking in the pd-ssh homebrew script) is done now. CORE-4937 (ripping fail2ban out of chef-managed EC2 instances) and CORE-4879 (helping Event Orchestration upgrade their mailservers) both got touched but are sitting blocked.

More layoff followup.

### Friday

Wrote up an incident review for Wednesday's wormly-sync crash ahead of a team meeting, then worked out the most important follow-ups given how little time the team has: adding `activeDeadlineSeconds` to the CronJob, escalating a "Check for Concurrent Builds" Buildkite step that's apparently been silently failing for about 12 days, and adding an image-existence check to the emergency deploy pipeline. Filed CORE-4997, CORE-4998, and CORE-4999 for those. Also started auditing which repos use the graduated-rollouts Buildkite pattern for the third one, but got tangled up in a zsh word-splitting bug partway through and left it unfinished.

Also filed CORE-5000 to retroactively track the repo-ownership spreadsheet from earlier in the week — should have made the ticket when I actually did the work.

Poked at the pd-ubuntu Docker images too, chasing down why a shell lands in a different directory depending on Ubuntu version. Turned out to be a WORKDIR difference — 20.04 uses `/`, 22.04 uses `/tmp`.

