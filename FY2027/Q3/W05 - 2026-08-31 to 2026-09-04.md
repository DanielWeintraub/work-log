# Work Log - 2026-08-31 to 2026-09-04

**Fiscal Year:** FY2027 Q3 Week 5

---

## Notes

### Monday

EKS Cluster Upgrades with RelEng

Also on-call follow up work to make our rotation a better experience.

### Tuesday

Spent most of the day on RLSE-1637, finishing the Kubernetes 1.35 upgrade for the two prod EKS clusters — eks-prod-pd-usw1 and eks-prod-pd-usw2. Same two-phase pattern as stg and EU prod: bump control plane and kube-proxy first, run the AMI smoke test, then bump the worker version and roll Karpenter's NodePools/EC2NodeClasses. usw1 went smoothly, PR #2172 merged. usw2 gave me some grief — Atlantis kept showing stale plan output from pagination and threw a "no matching plan status" error at one point, had to re-plan and re-apply to shake it loose. Worker bump across the five usw2 workload pools (arize_base, arize_db, default_workload_v2, lgtm_workload, network_debugging) went clean though, PR #2173 merged. Posted a summary comment on RLSE-1637 once both were done.

Also picked back up CORE-4997, adding the `activeDeadlineSeconds` passthrough to the cronjob helm chart from Friday's list. TDD'd it, full suite green, committed but haven't opened the PR yet.

Then dug into CORE-4998, the stuck graduated-rollout gate. Looked at extending release-engineering's buildkite-pipeline-unblocker Lambda but decided against modifying someone else's tool for this — brainstormed a couple of designs, they got overbuilt, ended up just writing a standalone bk-CLI script instead (sre-scripts/blocked-graduated-rollouts-check). Ran it and found 4 Core Infra pipelines currently stuck behind the same gate — atlas, buran, core-infra-service-test-deploy, and ec2-provisioning-tracker — blocked anywhere from 8 to 20 days. Script's still uncommitted; need to decide whether to triage those 4 pipelines first.

### Wednesday



### Thursday



### Friday


