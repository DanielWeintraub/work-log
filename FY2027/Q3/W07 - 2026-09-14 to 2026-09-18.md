# Work Log - 2026-09-14 to 2026-09-18

**Fiscal Year:** FY2027 Q3 Week 7

---

## Notes

### Monday

Spent the day on vuln tickets. Closed out CORE-4922 (govcloud) and CORE-4968 (prod), both critical/medium package updates from Wiz scans. Started on CORE-5034, another batch of critical findings on ops, mostly golang.org/x/crypto and pgx bumps.

Started working on a skill to automate some of this work.

### Tuesday

Added WarnWeakCrypto no-pq-kex to the pd-ssh config for CORE-5065. Our FIPS-validated OpenSSH on Ubuntu 22.04 has no PQ key exchange support yet, so this stops the warning. Bumped the formula to 3.1.3 and opened PR #202 on homebrew-pd_brews.

Also chased down a knife ssh_gateway bug for CORE-5066. knife ssh through knifeprod kept failing with HostKeyMismatch errors, while plain ssh to .pd hosts worked fine. Turned out the gateway hostnames in the knife-chef-config env files pointed at .pd-internal.com instead of the .pd aliases, so they took a different SSH path than ssm-ssh-proxy. Fixed all five env files (prod, stg, euprod, fedstg, fedprod) and opened PR #28 on knife-chef-config.

Approved the Bugsnag ServiceEntry PR for CORE-4760 on service-mesh-global-config.

### Wednesday



### Thursday

Spent the day testing the conmon-triage skill against real tickets, working CORE-5037, CORE-5029, CORE-5050, CORE-5053, and CORE-5026, all Wiz vulnerability findings. Wrapped up CORE-5037 (infra, otelcol-contrib CVEs) after confirming the 0.161.0 rollout fixes them, and moved it plus CORE-5029 to Ready for Review. Then dug into CORE-5050, chasing golang.org/x/mod and x/crypto CVEs in otelcol-contrib and cloudflared-fips on the GovCloud gw asset. Ran govulncheck against the actual binaries, filed CORE-5077 for a future cloudflared-fips upgrade, drafted a Deviation Request, and moved it to Ready for Review too. Started CORE-5053 (chef-dr) but didn't get through it. On CORE-5026, also chef-dr, the skill's first guess was wrong: it assumed otelcol-contrib 0.139.0, but the host actually runs 0.161.0, so all 27 CVEs turned out to already be fixed. Posted the evidence and flagged a version-pin mismatch in the knowledge base before committing anything.

Between tickets, kept tuning the skill itself. Added a Deviation Request form link to the output, dropped the step that spins up a separate remediation ticket, and added package-level grouping so a known binary with an upgrade already in flight, like otelcol-contrib, resolves as one group instead of CVE-by-CVE. Also poked at why CORE-5067 isn't showing up on the team board — looks like it's the Type of work field, still need to sort that out. Pushed a few commits to the skill branch.

### Friday


