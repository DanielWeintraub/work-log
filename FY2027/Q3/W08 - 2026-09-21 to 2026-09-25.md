# Work Log - 2026-09-21 to 2026-09-25

**Fiscal Year:** FY2027 Q3 Week 8

---

## Notes

### Monday



### Tuesday

Kept working ConMon tickets. Finished CORE-5034 on the ops workstation asset, 70 critical CVEs. Checked real package versions on the box via knifeprod ssh, and most of the findings turned out to be either already fixed (stale scans) or not applicable (the CVEs came from vendored requirements.txt/poetry.lock files bundled inside ansible collections that nothing on the host actually installs from). Wrote up all 70 as knowledge base entries and posted the Jira summary, but held off on pushing for now. Then wrapped CORE-5026 (chef-dr), another otelcol-contrib batch. Reconfirmed via SSH that the host is still on 0.161.0, so all 27 CVEs were already fixed, posted the comment, and moved it to Done.

Also picked up review feedback on the conmon-triage skill PR for CORE-5067. mdcollins05 flagged seven things: a test that wrote a real symlink into ~/.claude/skills, a dangling-symlink case in setup.sh, the validator silently accepting null version fields, some wording, and a couple of test assertions. Fixed all of them, ran the test suite, and pushed three commits.

Reviewed the renovate-base PR for CORE-5064, approved it, and left one comment about the top-level config in the doc.

### Wednesday



### Thursday

Closed out the rest of the cloudflared-fips ConMon batch: CORE-4906, CORE-4904, CORE-4902, CORE-4900, CORE-4898, and CORE-4896, covering infra, adminlb, ops, chef, chef-dr, and prod-infra. Pinned cloudflared-fips to 2026.9.3 in chef staging and updated the download script to match, then marked the rest of the batch Done behind that. Also cleared CORE-5081, the istiod HPA stuck at max replicas in eks-lt-pd-usw2, and CORE-5080, a non-actionable topoman not-running alert on new hosts. Finished off CORE-5067, the conmon-triage skill, too.

### Friday

Wrapped up the cloudflared-fips ConMon batch for good. Confirmed CVE-2025-69725 was actually fixed once all three prod-infra-gw hosts converged to cloudflared-fips 2026.9.3 behind chef PRs #18339-#18341, updated the knowledge base, and closed CORE-4906. Also fed a few things learned during the batch back into the conmon-triage skill itself: check a cookbook's own CLAUDE.md for a required staged rollout before touching a default.rb, don't file duplicate DRs when the same CVE shows up across a batch, and check a vendor's go.mod instead of pulling a binary off a host to verify a Go module version. Pushed that as PR #3 on infra-team-llm-config.

While chasing CORE-4906 I hit weird sudo failures on the prod-infra-gw hosts and dug into it in the chef repo. Turned out my account got faillock-locked on all three boxes - a session yesterday had run a fleet-wide knife ssh with sudo across role:gw, couldn't answer the password prompt, and that tripped the STIG lockout (three tries, no auto-unlock) on all three at once. The fix is just a faillock reset but needs root, so it's still open. Added a rule to CLAUDE.md requiring explicit go-ahead before any remote/fleet-wide command from now on so this doesn't happen again.

Also spent time on the gateway-infrastructure WARP routing PR (#83, FEDINF-1118), reviewing new gen-2 us-east-2 VPC CIDR routes for prod and staging. Approved it, but flagged the branch name for not following our ticket-prefix convention, and after the Atlantis plan came back realized the resources were actually federal/GovCloud tunnels rather than commercial - asked the author to confirm that was intended. Approved the companion sre-scripts PR (#220) too. Separately reviewed the Atlantis plan for the dependency-bump PR (#80) - all in-place version bumps, nothing destructive - called it safe to apply with a staged rollout, and flagged an unrelated pre-existing backend misconfig warning for release-eng.

